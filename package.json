{
  "name": "carillon",
  "version": "0.1.0",
  "private": true,
  "description": "A keystroke-rhythm sonifier. Typing becomes sound, in the browser. Nothing is uploaded.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\" && node verify.mjs ."
  },
  "engines": {
    "node": ">=20"
  }
}
